import React from 'react';
import ReactDOM from 'react-dom/client';
import TempConverter from './components/VideoPlayback';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <TempConverter />
  </React.StrictMode>
);
